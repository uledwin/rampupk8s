apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment
  labels:
    app: frontend-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend-app
  template:
    metadata:
      labels:
        app: frontend-app
    spec:
      containers:
      - name: frontend-app
        image: eultengodevacr01.azurecr.io/frontend
        env:
        - name: MI_VARIABLE
          value: "Mi_Prueba"
        - name: MI_OTRA_VARIABLE
          value: "MiSegundaVariable"
        - name: DD_AGENT_HOST
          valueFrom:
            fieldRef:
             fieldPath: status.hostIP
        resources:
          requests:
            memory: "64Mi"
            cpu: "200m"
          limits:
            memory: "128Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /  
            port: 3030
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          tcpSocket:
            port: 3030
          initialDelaySeconds: 15
          periodSeconds: 20
        ports:
        - containerPort: 3030
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  type: LoadBalancer
  ports: 
  - port : 3030
    targetPort: 3030
  selector:
    app: frontend-app